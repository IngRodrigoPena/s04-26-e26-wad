//import { SetColorTheme } from 'CC/lib';

interface Props {
  widthLogoIcon?: number;
  heightLogoIcon?: number;
}

export const LogoIcon = ({ widthLogoIcon = 30, heightLogoIcon = 30 }: Props) => {
  //const { normalCLogoIcon, inverseCLogoIcon } = SetColorTheme();
  return (
    
  );
};